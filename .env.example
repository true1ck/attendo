# Vendor Timesheet Tool Configuration
# Copy this file to .env and fill in your actual credentials

# Application Base URL (used in notification emails)
BASE_URL=http://localhost:5000

# Email Notification Settings (Gmail example)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# SMS Notification Settings (Optional - use any SMS API service)
SMS_API_URL=https://api.textlocal.in/send/
SMS_API_KEY=your-sms-api-key

# Database Configuration
DATABASE_URL=sqlite:///vendor_timesheet.db

# Development Settings
FLASK_ENV=development
FLASK_DEBUG=true

# Security
SECRET_KEY=your-secret-key-change-this-in-production

# Notes:
# 1. For Gmail, you need to enable 2FA and generate an "App Password"
# 2. SMS service is optional - notifications will still work via email only
# 3. Change all default credentials before deploying to production
# 4. Keep this file secure and never commit it to version control
