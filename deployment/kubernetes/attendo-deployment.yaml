apiVersion: apps/v1
kind: Deployment
metadata:
  name: attendo-app
  namespace: attendo
  labels:
    app: attendo
    component: web
spec:
  replicas: 3
  selector:
    matchLabels:
      app: attendo
      component: web
  template:
    metadata:
      labels:
        app: attendo
        component: web
    spec:
      containers:
      - name: attendo-app
        image: attendo:latest
        ports:
        - containerPort: 5000
        env:
        - name: FLASK_ENV
          value: "production"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: attendo-secrets
              key: database-url
        - name: SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: attendo-secrets
              key: secret-key
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 60
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 10
        volumeMounts:
        - name: uploads-storage
          mountPath: /app/uploads
        - name: reports-storage
          mountPath: /app/reports
      volumes:
      - name: uploads-storage
        persistentVolumeClaim:
          claimName: attendo-uploads-pvc
      - name: reports-storage
        persistentVolumeClaim:
          claimName: attendo-reports-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: attendo-service
  namespace: attendo
spec:
  selector:
    app: attendo
    component: web
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
  type: ClusterIP

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: attendo-ingress
  namespace: attendo
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
  - hosts:
    - attendo.mediatek-hackathon.com
    secretName: attendo-tls
  rules:
  - host: attendo.mediatek-hackathon.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: attendo-service
            port:
              number: 80
